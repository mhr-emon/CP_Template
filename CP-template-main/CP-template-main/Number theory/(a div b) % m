ll mod = 1000000007;
ll modPow(ll a, ll b, ll mod) {  //(a^b) % m
    ll res = 1;
    while (b) {
        if (b & 1) res = res * a % mod;
        a = a * a % mod; b >>= 1;
    }
    return res;
}
ll modDiv(ll a, ll b, ll mod) { //(a/b) % m
    return a * modPow(b, mod - 2, mod) % mod;  
